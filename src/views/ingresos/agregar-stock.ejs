<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Amora Inventarios</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="container">
        <div class="page-header">
            <h1>📦 Agregar Stock a Producto Existente</h1>
            <p>Selecciona un modelo y agrega más unidades de las tallas que necesites</p>
        </div>

        <div class="card">
            <% if (error) { %>
                <div class="alert alert-error">
                    <%= error %>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success">
                    <%= success %>
                </div>
            <% } %>

            <form method="POST" action="/ingresos/agregar-stock" class="form">
                <div class="form-group">
                    <label for="modelo">Modelo del Producto *</label>
                    <select 
                        name="modelo" 
                        id="modelo" 
                        class="form-control" 
                        required
                    >
                        <option value="">-- Selecciona un modelo --</option>
                        <% modelos.forEach(modelo => { %>
                            <option value="<%= modelo %>"><%= modelo %></option>
                        <% }) %>
                    </select>
                    <small class="form-text">Selecciona el modelo al que deseas agregar stock</small>
                </div>

                <div class="form-section">
                    <h3>📏 Agregar Tallas</h3>
                    <p class="form-text">Ingresa las cantidades que deseas agregar a cada talla (solo las tallas que quieras incrementar)</p>
                    
                    <div class="form-row">
                        <% for (let talla = 35; talla <= 40; talla++) { %>
                            <div class="form-group">
                                <label for="talla_<%= talla %>">Talla <%= talla %></label>
                                <input 
                                    type="number" 
                                    name="talla_<%= talla %>" 
                                    id="talla_<%= talla %>" 
                                    class="form-control talla-input"
                                    min="0"
                                    value="0"
                                    placeholder="0"
                                >
                            </div>
                        <% } %>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        ✅ Agregar Stock
                    </button>
                    <a href="/api/inventario" class="btn btn-secondary">
                        ❌ Cancelar
                    </a>
                </div>
            </form>
        </div>

        <div class="info-box">
            <h4>ℹ️ Información</h4>
            <ul>
                <li>Esta función <strong>suma</strong> las cantidades ingresadas al stock actual del modelo</li>
                <li>No es necesario ingresar cantidades en todas las tallas, solo en las que desees agregar</li>
                <li>El stock total del modelo se actualizará automáticamente</li>
                <li>Las cantidades con 0 no afectarán el stock existente</li>
            </ul>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script>
        // Validar que al menos una talla tenga cantidad > 0
        document.querySelector('form').addEventListener('submit', function(e) {
            const inputs = document.querySelectorAll('.talla-input');
            let tieneValor = false;
            
            inputs.forEach(input => {
                if (parseInt(input.value) > 0) {
                    tieneValor = true;
                }
            });

            if (!tieneValor) {
                e.preventDefault();
                alert('⚠️ Debes ingresar cantidad para al menos una talla.');
            }
        });

        // Auto-focus en el select al cargar
        document.getElementById('modelo').focus();
    </script>
</body>
</html>
