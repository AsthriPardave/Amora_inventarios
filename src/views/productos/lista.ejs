<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Amora Inventarios</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="container">
        <div class="page-header">
            <h1>📦 Lista de Productos</h1>
            <p>Todos los productos registrados en el inventario</p>
        </div>

        <% if (productos.length === 0) { %>
            <div class="alert alert-info">
                📭 No hay productos registrados aún.
            </div>
        <% } else { %>
            <div class="table-container">
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Modelo</th>
                            <th>Categoría</th>
                            <th>T35</th>
                            <th>T36</th>
                            <th>T37</th>
                            <th>T38</th>
                            <th>T39</th>
                            <th>T40</th>
                            <th>Total</th>
                            <th>Precio</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% productos.forEach(producto => { %>
                            <tr>
                                <td><%= producto.id %></td>
                                <td><strong><%= producto.modelo %></strong></td>
                                <td><%= producto.categoria %></td>
                                <td><%= producto.tallas[35] || 0 %></td>
                                <td><%= producto.tallas[36] || 0 %></td>
                                <td><%= producto.tallas[37] || 0 %></td>
                                <td><%= producto.tallas[38] || 0 %></td>
                                <td><%= producto.tallas[39] || 0 %></td>
                                <td><%= producto.tallas[40] || 0 %></td>
                                <td><strong><%= producto.totalUnidades %></strong></td>
                                <td><%= producto.precio %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>

        <div class="form-actions">
            <a href="/productos/registrar" class="btn btn-primary">+ Nuevo Producto</a>
            <a href="/" class="btn btn-secondary">← Volver</a>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="/js/main.js"></script>
</body>
</html>
