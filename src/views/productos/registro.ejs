<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Amora Inventarios</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="container">
        <div class="page-header">
            <h1>📦 Registrar Producto</h1>
            <p>Si el producto (modelo, marca, color, taco y precio) ya existe, se sumará el stock automáticamente. De lo contrario, se creará uno nuevo.</p>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>

        <div class="card">
            <form action="/productos/registrar" method="POST" class="form">
                <!-- Token único para prevenir duplicados -->
                <input type="hidden" name="formToken" value="<%= Date.now() + '-' + Math.random().toString(36).substr(2, 9) %>">
                <div class="form-section">
                    <h3>📅 Información Básica</h3>
                    
                    <div class="form-group">
                        <label for="fecha">Fecha *</label>
                        <input 
                            type="date" 
                            id="fecha" 
                            name="fecha" 
                            class="form-control"
                            value="<%= locals.formData?.fecha || new Date().toISOString().split('T')[0] %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="modelo">Modelo *</label>
                        <input 
                            type="text" 
                            id="modelo" 
                            name="modelo" 
                            class="form-control"
                            placeholder="Ej: Stiletto Elegante, Plataforma Casual"
                            value="<%= locals.formData?.modelo || '' %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="color">Color *</label>
                        <input 
                            type="text" 
                            id="color" 
                            name="color" 
                            class="form-control"
                            placeholder="Ej: Negro, Rojo, Beige"
                            value="<%= locals.formData?.color || '' %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="marca">Marca *</label>
                        <input 
                            type="text" 
                            id="marca" 
                            name="marca" 
                            class="form-control"
                            placeholder="Ej: Zara, Bata, Platanitos"
                            value="<%= locals.formData?.marca || '' %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="tamano_taco">Tamaño de Taco *</label>
                        <select 
                            id="tamano_taco" 
                            name="tamano_taco" 
                            class="form-control"
                            required
                        >
                            <option value="">Selecciona el tamaño</option>
                            <option value="5cm" <%= locals.formData?.tamano_taco === '5cm' ? 'selected' : '' %>>5 cm - Bajo</option>
                            <option value="7cm" <%= locals.formData?.tamano_taco === '7cm' ? 'selected' : '' %>>7 cm - Medio</option>
                            <option value="9cm" <%= locals.formData?.tamano_taco === '9cm' ? 'selected' : '' %>>9 cm - Alto</option>
                            <option value="11cm" <%= locals.formData?.tamano_taco === '11cm' ? 'selected' : '' %>>11 cm - Muy Alto</option>
                            <option value="Plataforma" <%= locals.formData?.tamano_taco === 'Plataforma' ? 'selected' : '' %>>Plataforma</option>
                            <option value="Stiletto" <%= locals.formData?.tamano_taco === 'Stiletto' ? 'selected' : '' %>>Stiletto</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="precio">Precio</label>
                        <input 
                            type="text" 
                            id="precio" 
                            name="precio" 
                            class="form-control"
                            placeholder="Ej: S/ 150.00"
                            value="<%= locals.formData?.precio || '' %>"
                        >
                    </div>

                    <div class="form-group">
                        <label for="descripcion">Descripción</label>
                        <textarea 
                            id="descripcion" 
                            name="descripcion" 
                            class="form-control"
                            rows="3"
                            placeholder="Características del producto"
                        ><%= locals.formData?.descripcion || '' %></textarea>
                    </div>
                </div>

                <div class="form-section">
                    <h3>👟 Stock Inicial por Talla</h3>
                    <p class="text-muted">Ingresa la cantidad inicial para cada talla</p>
                    
                    <div class="tallas-grid">
                        <% for(let talla = 35; talla <= 40; talla++) { %>
                            <div class="talla-input">
                                <label for="talla_<%= talla %>">Talla <%= talla %></label>
                                <input 
                                    type="number" 
                                    id="talla_<%= talla %>" 
                                    name="talla_<%= talla %>" 
                                    class="form-control"
                                    min="0"
                                    value="<%= locals.formData?.[`talla_${talla}`] || 0 %>"
                                    placeholder="0"
                                >
                            </div>
                        <% } %>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        ✓ Registrar Producto
                    </button>
                    <a href="/" class="btn btn-secondary">
                        ✕ Cancelar
                    </a>
                </div>
            </form>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="/js/main.js"></script>
</body>
</html>
