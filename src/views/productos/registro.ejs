<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Amora Inventarios</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="container">
        <div class="page-header">
            <h1>📦 Registrar Producto Nuevo</h1>
            <p>Agrega un nuevo producto al inventario</p>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>

        <div class="card">
            <form action="/productos/registrar" method="POST" class="form">
                <!-- Token único para prevenir duplicados -->
                <input type="hidden" name="formToken" value="<%= Date.now() + '-' + Math.random().toString(36).substr(2, 9) %>">
                <div class="form-section">
                    <h3>📅 Información Básica</h3>
                    
                    <div class="form-group">
                        <label for="fecha">Fecha *</label>
                        <input 
                            type="date" 
                            id="fecha" 
                            name="fecha" 
                            class="form-control"
                            value="<%= locals.formData?.fecha || new Date().toISOString().split('T')[0] %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="modelo">Modelo *</label>
                        <input 
                            type="text" 
                            id="modelo" 
                            name="modelo" 
                            class="form-control"
                            placeholder="Ej: Nike Air Max 2024"
                            value="<%= locals.formData?.modelo || '' %>"
                            required
                        >
                    </div>

                    <div class="form-group">
                        <label for="categoria">Categoría</label>
                        <input 
                            type="text" 
                            id="categoria" 
                            name="categoria" 
                            class="form-control"
                            placeholder="Ej: Zapatillas, Sandalias, Botas"
                            value="<%= locals.formData?.categoria || 'Zapatillas' %>"
                        >
                    </div>

                    <div class="form-group">
                        <label for="precio">Precio</label>
                        <input 
                            type="text" 
                            id="precio" 
                            name="precio" 
                            class="form-control"
                            placeholder="Ej: S/ 150.00"
                            value="<%= locals.formData?.precio || '' %>"
                        >
                    </div>

                    <div class="form-group">
                        <label for="descripcion">Descripción</label>
                        <textarea 
                            id="descripcion" 
                            name="descripcion" 
                            class="form-control"
                            rows="3"
                            placeholder="Características del producto"
                        ><%= locals.formData?.descripcion || '' %></textarea>
                    </div>
                </div>

                <div class="form-section">
                    <h3>👟 Stock Inicial por Talla</h3>
                    <p class="text-muted">Ingresa la cantidad inicial para cada talla</p>
                    
                    <div class="tallas-grid">
                        <% for(let talla = 35; talla <= 40; talla++) { %>
                            <div class="talla-input">
                                <label for="talla_<%= talla %>">Talla <%= talla %></label>
                                <input 
                                    type="number" 
                                    id="talla_<%= talla %>" 
                                    name="talla_<%= talla %>" 
                                    class="form-control"
                                    min="0"
                                    value="<%= locals.formData?.[`talla_${talla}`] || 0 %>"
                                    placeholder="0"
                                >
                            </div>
                        <% } %>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        ✓ Registrar Producto
                    </button>
                    <a href="/" class="btn btn-secondary">
                        ✕ Cancelar
                    </a>
                </div>
            </form>
        </div>

        <div class="info-box">
            <h4>💡 Información</h4>
            <ul>
                <li>El producto se agregará al inventario en Google Sheets</li>
                <li>Puedes dejar tallas en 0 si no tienes stock inicial</li>
                <li>El total se calculará automáticamente</li>
            </ul>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="/js/main.js"></script>
</body>
</html>
